import React from 'react';

const Loading = () => {
    return (
        <div>
            <p className='text-center text-4xl text-blue-800'><button className="btn loading">loading</button></p>
        </div>
    );
};

export default Loading;